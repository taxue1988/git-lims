# Django LIMS项目依赖包
# 核心框架
Django==4.2.7

# 数据库相关
mysqlclient==2.1.1

# 可选：如果需要使用SQLite（开发环境）
# sqlite3 是Python内置模块，无需安装

# 开发工具（可选）
# django-debug-toolbar==4.2.0  # 开发环境调试工具
# django-extensions==3.2.3     # Django扩展工具

# 生产环境推荐
gunicorn==21.2.0              # WSGI服务器
whitenoise==6.6.0             # 静态文件服务

# 安全相关
python-decouple==3.8          # 环境变量管理
django-cors-headers==4.3.1   # CORS支持（如果需要API）

# 数据处理（如果需要）
pandas==2.1.3               # 数据分析（训练使用）
numpy==1.25.2               # 数值计算（训练使用）
scikit-learn==1.3.2         # 机器学习（训练使用）
# xgboost==1.7.6             # 可选：XGBoost（如需使用则取消注释）
# lightgbm==4.1.0            # 可选：LightGBM（如需使用则取消注释）

# 其他常用包
Pillow==10.1.0               # 图像处理（如果需要上传图片）
python-dateutil==2.8.2       # 日期处理工具

# 部署相关
psycopg2-binary==2.9.9       # PostgreSQL支持（如果需要）
redis==5.0.1                  # Redis缓存（如果需要）

# 测试工具（可选）
# pytest==7.4.3
# pytest-django==4.7.0
# coverage==7.3.2

# API 请求
requests==2.32.5

# WebSocket支持
channels==4.3.1
daphne==4.2.1
